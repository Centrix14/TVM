* This demonstrates how to summarize the values of the entire stack and extract the values from it *

.main
push 2 * Put the value 2 on the k *stac
push 3 * Put the value 3 on the stack *
put _stack _stdi * Display the stack value as a number on the screen *
eject * Extract the element from the stack *
put _stack _stdi * Display the stack value as a number on the screen *
.end